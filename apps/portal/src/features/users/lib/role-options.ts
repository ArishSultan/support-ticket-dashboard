/** Assignable user roles. `role` is a free-text column, so these are the app's convention. */
export const ROLE_OPTIONS: Array<{ value: string; label: string }> = [
  { value: 'agent', label: 'Agent' },
  { value: 'admin', label: 'Admin' },
];
